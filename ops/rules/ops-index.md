---
name: ops-index
description: Index of the ops rulebook entries.
alwaysApply: true
---

ops rules, read on demand via `rule://<name>`:

- `ops-toolchain-cache-policy` — shared toolchain caches, worktree-local build output, disk-pressure eviction.
