---
name: terraform-index
description: Index of the terraform rulebook entries.
alwaysApply: true
---

terraform rules, read on demand via `rule://<name>`:

- `terraform-language` — stacks, modules, and HCL conventions when editing `*.tf`/`*.hcl`.
