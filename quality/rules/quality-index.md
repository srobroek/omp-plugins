---
name: quality-index
description: Index of the quality rulebook entries.
alwaysApply: true
---

quality rules, read on demand via `rule://<name>`:

- `quality-browser-verification` — when layout, interaction, rendering, or user-visible state must be checked in a browser.
