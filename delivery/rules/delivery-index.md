---
name: delivery-index
description: Index of the delivery rulebook entries.
alwaysApply: true
---

delivery rules, read on demand via `rule://<name>`:

- `delivery-cadence` — committing, pushing, or stopping with unpushed work.
- `delivery-git-workflow` — branching, PRs, merge proof, beads merge-queue linkage.
