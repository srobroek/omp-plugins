---
name: session-index
description: Index of the session rulebook entries.
alwaysApply: true
---

session rules, read on demand via `rule://<name>`:

(none — this plugin ships skills `catchup` and `handover` only)
