import {
	type DepRow,
	Detector,
	detectProject,
	isDir,
	isFile,
	MISSING,
	parseRequirement,
} from "./shared-detector";

export type { DepRow };
export { Detector, detectProject, isDir, isFile, MISSING, parseRequirement };
