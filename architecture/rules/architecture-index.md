---
name: architecture-index
description: Index of the architecture rulebook entries.
alwaysApply: true
---

architecture rules, read on demand via `rule://<name>`:

- `architecture-compose-dont-fork` — new cases compose on the generic core.
- `architecture-top-level` — capability-first top-level directories.
- `architecture-ownership` — libs roles, owner-local assets, no root agents/.
- `architecture-docs-files` — durable knowledge lives under docs/.
- `architecture-component-promotion` — promote UI to libs/ui after two reuses.
- `architecture-structural-search` — lsp and ast_grep instead of Serena.
