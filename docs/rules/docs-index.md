---
name: docs-index
description: Index of the docs rulebook entries.
alwaysApply: true
---

docs rules, read on demand via `rule://<name>`:

- `docs-spec-workflow` — specs/, SpecKit modes, or `.specify/` assets.
