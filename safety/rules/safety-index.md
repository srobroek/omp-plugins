---
name: safety-index
description: Index of the safety rulebook entries.
alwaysApply: true
---

safety rules, read on demand via `rule://<name>`:

- `srobroek-attribution-guard` — git commits with AI attribution trailers.
- `srobroek-bash-indirection-guard` — bash that hides the real command.
- `srobroek-git-force-push-advisory` — force-push / history rewrite.
- `srobroek-remote-exec-guard` — remote fetch piped into a shell.
- `srobroek-sudo-destructive-advisory` — sudo plus destructive paths.
