---
name: go-index
description: Index of the go rulebook entries.
alwaysApply: true
---

go rules, read on demand via `rule://<name>`:

- `go-language` — Go layout, package boundaries, and tooling when editing `*.go`.
