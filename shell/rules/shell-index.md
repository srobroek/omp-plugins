---
name: shell-index
description: Index of the shell rulebook entries.
alwaysApply: true
---

shell rules, read on demand via `rule://<name>`:

- `shell-language` — quoting, portability, and command safety when editing `*.sh`/`*.bash`/`*.zsh`.
