{
  "name": "@srobroek/worktrunk",
  "version": "2.0.0",
  "description": "Refuse OMP native isolation, because a cloned .beads forks the embedded ledger. Worktree discipline itself is steering.",
  "private": true,
  "omp": {
    "extensions": [
      "./extensions/isolation-precheck.ts"
    ]
  }
}