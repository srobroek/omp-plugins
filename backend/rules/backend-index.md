---
name: backend-index
description: Index of the backend rulebook entries.
alwaysApply: true
---

backend rules, read on demand via `rule://<name>`:

- `backend-runtime` — services, functions, workers, and deployable ownership.
- `backend-api-contracts` — API, schema, and owner-local contract boundaries.
- `backend-background-jobs` — queues, events, retries, and worker semantics.
