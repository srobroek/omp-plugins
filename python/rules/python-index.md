---
name: python-index
description: Index of the python rulebook entries.
alwaysApply: true
---

python rules, read on demand via `rule://<name>`:

- `python-language` — Python layout, domain boundaries, and tooling when editing `*.py`.
