# Recurring catalog gates for every accepted internationalization library.

# Install or prepare each catalog tool in a clean checkout
[group('i18n')]
i18n-prepare:
    #!/usr/bin/env bash
    set -euo pipefail
@@I18N_PREPARE_COMMANDS@@

# Reject missing, orphaned, or invalid messages
[group('i18n')]
i18n:
    #!/usr/bin/env bash
    set -euo pipefail
@@I18N_CHECK_COMMANDS@@
