---
name: rust-index
description: Index of the rust rulebook entries.
alwaysApply: true
---

rust rules, read on demand via `rule://<name>`:

- `rust-crate-boundaries` — crate splits, lib.rs facades, adapter extraction.
- `rust-domain-modeling` — thiserror scope, UUID v5 identity, serde newtypes.
- `rust-contract-boundary` — generated bindings, invoke names, wire casing.
- `rust-safe-mutation` — plan/approve/apply, TOCTOU CAS, path traversal.
- `rust-ci` — rust-cache, required ci-gate, supply-chain attestation.
- `rust-persistence` — sqlx repo, numbered migrations, CAS-in-transaction.
- `rust-workspace` — workspace lints, deps, feature-gated dev surface.
- `rust-errors` — wire error-code registry, envelope, append-only audit.
- `rust-tauri` — Tauri v2 bundles, updater, signing, WebDriver E2E.
