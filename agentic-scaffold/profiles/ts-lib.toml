name = "ts-lib"
summary = "Bun-managed TypeScript library"
layers = ["base", "tooling", "hooks", "lang/ts", "ci/github", "release", "agentic"]
[vars]
language = "ts"
app = false
node = "22"
bun_version = "latest"
web_ui = false
speckit = false
[commands]
setup = "mise install && bun install"
test = "bun test"
lint = "bunx oxlint"
fmt = "bunx biome check ."
check = "bunx tsc --noEmit"
kind = "library"
