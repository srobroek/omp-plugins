name = "monorepo"
summary = "Workspace repository with nested language members"
layers = ["base", "tooling", "hooks", "workspace", "ci/github", "release", "agentic"]
[vars]
layout = "monorepo"
language = "none"
bts = "false"
bts_version = "3.42.2"
bts_layout = "turborepo"
web_ui = false
speckit = false
[commands]
setup = "mise install"
test = "echo workspace members"
lint = "echo workspace members"
fmt = "echo workspace members"
check = "just check"
