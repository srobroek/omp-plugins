name = "release"
summary = "release-please configuration and workflow"
after = ["ci/github"]
requires_tools = ["gh"]
owns = ["release-please-config.json", ".release-please-manifest.json", ".github/workflows/release-please.yml"]
blocks = []
conflicts_with = []
[tools]
gh = "latest"
