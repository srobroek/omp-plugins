[group('rust')]
rust-test:
    cargo test

[group('rust')]
rust-lint:
    cargo clippy --all-targets --all-features -- -D warnings

[group('rust')]
rust-fmt:
    cargo fmt --all

[group('rust')]
rust-check:
    cargo check --all-targets
