# hooks

Provides prek configuration for commit and context lifecycle stages.

Run `scaffold.py hooks install` after render. If a global `core.hooksPath` is configured, choose `--force` for repository hooks, move the setting to repository scope, or skip hooks.
