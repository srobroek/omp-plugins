name = "ci/github"
summary = "GitHub CI with parallel lanes and a single fan-in gate, plus Renovate config"
after = ["base"]
requires_tools = ["gh"]
owns = [".github/workflows/ci.yml", ".github/renovate.json"]
blocks = []
conflicts_with = []
[tools]
gh = "latest"
