# Brownfield

Run `inspect` first. Resolve multiple hook managers, existing instructions, and missing tools before rendering. Plan must classify existing files as `skip`, `update-block`, or `conflict`; a conflict is a stop, not an overwrite permission. Render the `agentic` and explicitly selected layers, then run plugin sync, hooks install, and `just check`.
